/*
int Menu_Navigation()
{
  if(gx > 40) //Down
  {
    MenuCursor++;           // Suggestion is to make it a modulus calculation where it is mod the number of items in the menu, then it can go around the items
  }
  else if(gx < -40) //Up
  {
    MenuCursor--;
  }
}
*/
